// For each step or argument adds a shouldBeNewlined property, as well as the line's indentation
const formatSyntaxTree = (config = { maxLineLength: 80 }) => (syntaxTree) => {
  return syntaxTree;
};

module.exports = { formatSyntaxTree };
